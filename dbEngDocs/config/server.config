 /*
 CONFIG FILE

This config is intended to contain the various configuration files for the database.
*/

{
    "fileDetails": {
        "DB": "dbEngDocs",
        "collection":"serverSetup",
        "file": ".config"},
    "serverAddress": "LOCALHOST",
    "port": [INSERT PORT NUMBER HERE]
}
